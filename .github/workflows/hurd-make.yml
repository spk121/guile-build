name: Hurd Build

on:
  schedule:
    - cron: '0 14 * * *'  # Daily at 14:00 UTC
  workflow_dispatch:
    inputs:
      repository:
        description: 'Repository to build from'
        required: true
        default: 'https://codeberg.org/guile/guile.git'
        type: choice
        options:
          - 'https://codeberg.org/guile/guile.git'
          - 'https://github.com/spk121/guile.git'
      ref:
        description: 'Branch, tag, or commit SHA (leave blank for default branch)'
        required: false
        default: ''
        type: string

jobs:
  build:
    name: Build and Install
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: read
    steps:
    - name: Clone source repository
      env:
        REPO_URL: ${{ github.event_name == 'workflow_dispatch' && inputs.repository || 'https://codeberg.org/guile/guile.git' }}
        REPO_REF: ${{ github.event_name == 'workflow_dispatch' && inputs.ref || '' }}
      run: |
        git clone "$REPO_URL" --depth=1
        if [ -n "$REPO_REF" ]; then
          cd guile
          git fetch --depth=1 origin "$REPO_REF"
          git checkout "$REPO_REF"
        fi
      shell: bash

    # Build and install run in a single VM step. The Hurd VM only has SCP
    # available for copying files out, so each phase's log is explicitly
    # copied back after the phase runs (success or failure). The final
    # exit propagates any failure so the workflow status reflects it.
    - name: Build and Install in Debian GNU/Hurd VM
      uses: spk121/hurd-vm@v1
      with:
        sync: scp
        cpu: 4
        prepare: |
          apt-get update
          apt-get install -y --no-install-recommends \
            build-essential \
            texlive-base \
            texinfo \
            autoconf \
            automake \
            m4 \
            llvm \
            libtool \
            gettext \
            gperf \
            pkg-config \
            lzip \
            libgc-dev \
            libgmp-dev \
            libffi-dev \
            libltdl-dev \
            libunistring-dev \
            flex \
            gperf \
            libreadline-dev \
            ca-certificates \
            curl \
            autopoint
        run: |
          set -u
          export LANG=C.UTF-8
          mkdir -p "$(pwd)/app"
          cd guile

          set -o pipefail

          # Configure phase
          ./autogen.sh 2>&1 | tee configure.log
          autogen_exit=$?
          ./configure CFLAGS="-g -O2 -Wall" --prefix="$(pwd)/../app" --enable-mini-gmp 2>&1 | tee -a configure.log
          configure_exit=$?

          # Copy configure logs back regardless of outcome
          scp configure.log . 2>/dev/null || true
          scp config.log . 2>/dev/null || true

          # Build phase (only if configure succeeded)
          build_exit=0
          if [ $autogen_exit -eq 0 ] && [ $configure_exit -eq 0 ]; then
            make -j"$(nproc)" V=1 2>&1 | tee build.log
            build_exit=$?
          fi
          scp build.log . 2>/dev/null || true

          # Install phase (only if build succeeded)
          install_exit=0
          if [ $build_exit -eq 0 ]; then
            make install V=1 2>&1 | tee install.log
            install_exit=$?
          fi
          scp install.log . 2>/dev/null || true

          # Verify installation
          if [ $install_exit -eq 0 ]; then
            export PATH="$(pwd)/../app/bin:$PATH"
            ls -la "$(pwd)/../app/bin/"
            echo "--- guile --version ---"
            guile --version
            echo "--- guile-config info ---"
            guile-config info || true
            echo "--- evaluate (display (+ 1 2)) ---"
            guile -c '(display (+ 1 2)) (newline)'
          fi

          # Propagate any failure
          if [ $autogen_exit -ne 0 ] || [ $configure_exit -ne 0 ] || [ $build_exit -ne 0 ] || [ $install_exit -ne 0 ]; then
            exit 1
          fi

    - name: Upload Configure Log
      if: success() || failure()
      uses: actions/upload-artifact@v6
      with:
        name: configure-log
        path: |
          configure.log
          config.log
          guile/configure.log
          guile/config.log
        if-no-files-found: ignore

    - name: Upload Build Log
      if: success() || failure()
      uses: actions/upload-artifact@v6
      with:
        name: build-log
        path: |
          build.log
          guile/build.log
        if-no-files-found: ignore

    - name: Upload Install Log
      if: success() || failure()
      uses: actions/upload-artifact@v6
      with:
        name: install-log
        path: |
          install.log
          guile/install.log
        if-no-files-found: ignore
